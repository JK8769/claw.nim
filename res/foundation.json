{
  "schema_version": 1,
  "description": "Foundation-tier skills shipped with the claw distribution. Every company gets these auto-mirrored into its foundation/skills/ dir on `claw co create` / `claw co update`. Content lives under res/foundation/<name>/.",

  "skills": {
    "tools": {
      "path": "res/foundation/tools",
      "description": "Manage your capability surface: discover hidden tools by intent, delegate to peers, forge new MCP tools when nothing fits, capture reusable workflows as skills. Universal — granted to every agent.",
      "requires": {
        "tools": ["find_tools", "mcp", "delegate", "skill", "file", "shell"],
        "deps": [{"package": "nim", "manager": "system"}],
        "env": []
      }
    },
    "forge_nim_expert": {
      "path": "res/foundation/forge_nim_expert",
      "description": "How to use forge_mcp_tool to create runtime MCP tools in Nim. Read this BEFORE calling forge_mcp_tool — gets the macro shape, server-instance pattern, and stdio transport boilerplate right on the first try.",
      "requires": {
        "tools": ["mcp"],
        "deps": [{"package": "nim", "manager": "system"}],
        "env": []
      }
    }
  }
}
